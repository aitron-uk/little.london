export {
  DURATION_PRESETS,
  addDays,
  buildHourSlots,
  buildTimeSlots,
  endTime,
  findOverlap,
  formatDayLabel,
  formatDuration,
  formatFriendlyDate,
  formatShortDay,
  formatTime12,
  getClientSuggestions,
  getNextAppointment,
  isSameDay,
  minutesToTime,
  parseDateKey,
  rangesOverlap,
  startOfWeek,
  timeToMinutes,
  toDateKey,
} from '../../shared/time.ts'

export type { ClientSuggestion } from '../../shared/time.ts'
